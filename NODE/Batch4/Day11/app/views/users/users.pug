doctype html
html(lang='en')
  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge,chrome=1')
    title User Detail
  body
    section.container
      for user in users
        P #{user.name}
        P #{user.email}
        p
         a(href='/api/v1/user/'+user._id) details